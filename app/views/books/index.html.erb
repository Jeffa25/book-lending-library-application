<h1>Books</h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book_path(book) %>
      <%= book.borrowings.where(returned_at: nil).exists? ? "(Borrowed)" : "(Available)" %>
    </li>
  <% end %>
</ul>